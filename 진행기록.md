# 진행기록 — 인투마케팅 홈페이지 · 어드민

> 이 저장소(`intoedu/intomarketing`)는 **공개 저장소**입니다.
> 비밀번호·비밀키는 여기에 절대 적지 마세요. 아래 적힌 키는 공개용(publishable)뿐입니다.

---

## 2026-09-21 — 어드민에서 주문·견적서를 직접 만들 수 있게 했습니다

### 왜

지금까지 견적서는 **홈페이지로 신청이 들어와야만** 쓸 수 있었습니다.
전화·방문·소개로 들어온 건은 어드민에 넣을 방법이 아예 없었습니다.
9/28부터 외부 방문판매를 시작하므로, 현장에서 받은 건을 바로 넣고
견적서를 뽑을 수 있어야 합니다.

### 무엇을 넣었나

**① 목록 오른쪽 위 `+ 직접 주문 만들기`** (두 어드민 모두)

| | 인투마케팅 `/marketing/admin/` | 학원 `/hakwon/admin.html` |
|---|---|---|
| 고르는 것 | 작업 묶음 (홈페이지·SNS·디자인·영상·블로그·기타) | 지원 항목 (자료제작·인력·컨텐츠·마케팅) |
| 들어가는 값 | `kind` + `area` 를 같이 넣음 | `area` 가 묶음, 제작이면 `kind`=작업 종류 |

받는 칸 = 상호명(필수) · 담당자 · 연락처 · 이메일 · **사업자등록번호** · 주소 ·
접수 경로(전화/방문/카톡/메일/소개/기타) · 작업 종류 · 요청 내용.
[만들고 견적서 쓰기] 를 누르면 만들자마자 견적서 창이 열립니다.

**② 견적서에 칸 네 개 추가** (두 어드민 모두)

- 받는 곳 — **사업자등록번호 · 주소** (값이 있을 때만 인쇄됩니다)
- 견적 정보 — **납품 기한 · 우리 쪽 담당자** (역시 있을 때만 인쇄)
- **결제 조건** — 결제 방법·시기 + 입금 계좌를 **견적서마다** 따로 적습니다

### 🔴 다음 사람이 알아야 할 것

**1. 넣기(insert)는 손님 신청서와 규칙이 같습니다.**
DB 규칙 `anyone_can_insert_request` 가 이렇게 막고 있습니다 —
`status='신청접수'` · 담당자·금액·메모·단계·견적서가 **전부 비어 있어야** 넣어집니다.
그래서 「직접 주문 만들기」 창에는 **금액·담당자·마감 칸이 없습니다.**
넣은 뒤 상세 화면에서 정하시면 됩니다. 이 창에 금액 칸을 붙이면 **넣기가 통째로 실패합니다.**

**2. 결제 조건은 「이 견적서에 적은 것 → 없으면 견적서 설정의 기본 문구」 순서입니다.**
`payHTML()` 한 곳에서 정합니다. 둘 다 비면 그 칸을 **아예 안 그립니다**
(빈 네모가 인쇄되면 이상해서). 칸 자체를 없애려면 여기만 고치면 됩니다.

**3. DB 는 하나도 안 건드렸습니다.**
사업자등록번호·주소·납품 기한·결제 조건은 전부 `requests.quote`(jsonb) 안에 들어갑니다.
직접 주문의 사업자등록번호·주소·접수 경로는 `requests.details` 안에 들어갑니다
(`등록 경로` = `관리자 직접 입력` 으로 표시해 둡니다 — 홈페이지 신청과 구분하려고).
칸 추가도 없었습니다.

**4. 옛 견적서는 그대로 열립니다.** 새 칸이 없으면 빈칸으로 두고 인쇄에서 빠집니다.

### 🔧 같은 날 고침 — 견적 항목 금액 칸

**증상** : 개당가격에 숫자를 칠 때마다 커서가 빠져나가 한 자리씩 다시 눌러야 했고, `1,500,000` 처럼 쉼표를 치면 인식이 안 됐습니다.

**원인** : ① 개당가격·수량을 한 글자 고칠 때마다 `renderQuoteRows()` 가 **표 전체를 다시 그려서** 칸이 새로 만들어졌습니다(=커서 사라짐). ② 칸이 `type="number"` 라 쉼표를 못 받았습니다.

**고친 것** : 금액 칸 네 개(개당가격·수량·기존 비용·할인가)를 글자 칸 + `qNum()` 으로 바꿈. 숫자만 남기고 쉼표를 자동으로 찍고, 커서 자리를 지킵니다. 할인가는 **그 칸만** 바꾸고 표는 다시 그리지 않습니다. 🔴 **저장값은 예전과 같이 쉼표 없는 숫자**(`"1500000"`)라 옛 견적서·합계 계산은 그대로입니다. 금액 칸을 다시 고칠 때 `renderQuoteRows()` 를 입력 중에 부르지 마세요 — 같은 증상이 돌아옵니다.

### 🔧 같은 날 또 고침 — 견적 항목 입력 순서 (이은총 님 요청)

- 개당가격 × 수량 → 이제 **기존 비용**에 들어갑니다 (예전엔 할인가).
- **수량에서 Tab** → 결제주기를 건너뛰고 **기존 비용**으로 갑니다.
- **기존 비용에서 Tab** → 같은 금액이 **할인가(청구)**에 들어가고 그리로 갑니다. 바로 숫자를 치면 할인가를 덮어씁니다.
- 🔴 할인가를 **따로 깎아 둔 줄**은 Tab 을 다시 눌러도 안 덮어씁니다. 할인가가 비어 있거나 기존 비용과 같을 때만 넘깁니다(`qFocus` 가 들어올 때의 기존 비용을 기억 → `qKey` 에서 비교).
- 결제주기(1회/매월/매년)는 Tab 으로 안 거치므로 마우스로 고릅니다. Shift+Tab 은 평소처럼 뒤로 갑니다.

### 🔧 같은 날 세 번째 — 금액 칸을 «엑셀 방식»으로 (이은총 님 제보)

**증상** : `30,000` 에서 3 을 지우니 뒤의 000 까지 사라짐.
**원인** : 한 글자마다 쉼표를 다시 찍고 있었음 → `0,000` 을 숫자로 바꾸면 `0`.
**고친 것** — 이제 규칙은 이렇습니다.

1. **치는 동안에는 칸 글자를 절대 안 고칩니다.** 쉼표는 칸을 벗어날 때(`qBlur`) 찍습니다. 쉼표를 쳐도 되고 안 쳐도 됩니다.
2. 개당가격 × 수량 → 기존 비용. 단 **기존 비용을 직접 다른 값으로 적었으면 안 건드립니다.** 우리가 곱해 넣은 값을 `it._auto` 에 기억해 비교합니다(지우는 도중 잠깐 0 이 돼도 흐름이 안 끊기게). `_auto` 는 저장 전에 지웁니다.
3. **할인가가 기존 비용과 같으면(=할인 없음) 기존 비용을 따라갑니다.** 다르게 적은 할인가는 그대로 둡니다.
4. **저장할 때 빈칸 채우기** : 기존 비용만 있으면 할인가=기존 비용 / 할인가만 있으면 기존 비용=할인가 / 둘 다 없고 개당가격×수량이 있으면 둘 다 그 값.

🔴 시험 함정 : 이 시험 브라우저에서는 **Backspace 키가 어떤 칸에서도 안 먹습니다**(메모 칸도 마찬가지 — 도구 문제). 지우기 시험은 `document.execCommand('delete')` 로 하세요. 또 스크립트로 `blur()` 하면 칸 벗어남 신호가 안 나서 쉼표가 안 찍혀 보입니다 — 쉼표 확인은 실제 Tab 키로.

### 어떻게 확인했나

로그인·DB 없이 화면만 열리는 **시험판**을 만들어(가짜 supabase 를 끼움) 두 어드민 모두
「직접 주문 만들기 → 견적서 → 인쇄 미리보기」를 실제로 눌러 봤습니다.
사업자등록번호·주소·납품 기한·담당자·결제 조건·입금 계좌가 인쇄본에 다 찍히는 것,
결제 조건을 적었을 때 **설정의 기본 문구가 안 나오는 것**까지 확인했습니다.
콘솔 오류 없음. 시험판은 저장소에 넣지 않았습니다(임시 폴더에만 있음).

---

## 2026-09-20 — 매뉴얼대로 가격을 바꾸고, 신청서 네 장을 만들었습니다

### 🔴 홈페이지 가격이 완전히 바뀌었습니다

근거 = `인투마케팅/인투마케팅 메뉴얼/홈페이지매뉴얼.html`

| | 전 | 후 |
|---|---|---|
| 단계 | 4단계 | **3단계** (단순 홍보형 / 소통형 / 맞춤 제작형) |
| 돈 | 제작비 + 월 관리비 | **제작비 없음. 약정 월 요금만** |

```
1단계  3개월 65,000 · 6개월 55,000 · 12개월 48,000 · 24개월 45,000
2단계  3개월 103,000 · 6개월 88,000 · 12개월 76,000 · 24개월 72,000
3단계  견적 후 안내
페이지  1단계 3장 이하 · 2단계 5장 이하 · 3단계 10장 이상 (게시판·로그인 화면은 안 셈)
```

부가세 포함 · 오픈일부터 매달 자동결제 · 최소 약정 3개월.

**ESC 파트너 혜택이 바뀌었습니다** — 예전에는 「제작비 0원」이 파트너 혜택이었는데 이제 제작비는 누구나 없습니다.
2026-09-20 이은총 님 결정 = **파트너는 약정 기간과 상관없이 24개월 값**(1단계 45,000 / 2단계 72,000).
`hakwon/index.html` 의 FAQ 2곳·통계 칸·위쪽 안내 줄도 같이 고쳤습니다.

🔴 **매뉴얼 안에서 한 군데 어긋납니다** — 「가격」 장은 제작비 없음인데
뒤쪽 **견적서 양식에는 「제작비」·「처음 낼 금액」 칸**이 남아 있습니다. 견적서 만들 때 확인 필요.

### 신청서 — 공통 껍데기 + 네 장

`apply/form.css` + `apply/form.js` 를 네 장이 같이 씁니다. 각 신청서는 **내용만** 적습니다.

| 표시 | 하는 일 |
|---|---|
| `data-col="academy_name"` | requests 표의 그 칸으로 바로 |
| `data-q="물음 이름"` | details(jsonb) 안에 그 이름으로 |
| `data-multi` / `data-one` | 여러 개 / 하나만 고르는 묶음 |
| `data-qty="이름"` | 수량 칸. 0이면 안 담음 |
| `data-req="보일 이름"` | 비면 못 내게 막음 |

| 주소 | kind | area |
|---|---|---|
| `/apply/` | 홈페이지 | 제작 |
| `/apply/design/` | 디자인 | 제작 |
| `/apply/video/` | 영상 | 제작 |
| `/apply/blog/` | 블로그·리뷰 | 마케팅 |
| `/apply/sns/` | SNS | 마케팅 |
| `/hakwon/apply/` | 고른 값 | 제작 (center=esc) |

🔴 **블로그 신청서는 업종을 고르면 「주로 오는 고객」과 「홍보 목적」 보기가 바뀝니다.**
근거 = `학원지원센터/신청서/블로그마케팅-신청서-내용정리.md` **부록 B** — 그 두 가지는 학원 전용이라
업종마다 새로 짜야 한다고 적혀 있습니다. 업종을 늘리려면 `apply/blog/index.html` 의 `BIZ` 만 고치면 됩니다.

### 그 밖에

- 홈(`/`)에 **분류 트리** — 왼쪽 「전체 + 5항목 + 하위 10」, 누르면 그 갈래로 이동 (참고사진 15·16번)
- 디자인을 **세 갈래**로 — 로고·명함 / 인쇄·홍보물 / SNS·카드뉴스
- 🔴 **디자인은 매뉴얼이 없습니다.** 가격·환불·작업순서 근거가 없어 소개와 신청서만 만들었습니다.
  옵션 추가(추가 시안·추가 수정·원본 파일·로고 가이드)는 **개수만** 받고 금액은 견적서에서 안내합니다.

### 영상 · 블로그 · SNS 화면을 매뉴얼로 채움 (2026-09-20)

| 화면 | 넣은 것 | 근거 |
|---|---|---|
| 영상 | 가격표 · 진행 순서 6단계 · 포함 안 되는 것 · 환불 | `영상/0_운영_기본_규칙.pdf` 4장, `1_제작_진행`, `3_환불` |
| SNS | 채널 구분 6 · 정기 콘텐츠 한 달 흐름 · 계정 명의 · 심사기간 · 환불 | `SNS/매뉴얼0~3.docx` |
| 블로그 | 편수 10~100 / 30일 · 진행 순서 · 검색어 변경 3,000원 · 마감 못 지킬 때 · 중도 해지 | `블로그-담당자-매뉴얼-0915.html` |

🔴 **SNS 가격은 일부러 안 올렸습니다.** 매뉴얼 가격표 위에
「시장가 조사(2026년 9월) 기준이며 **원가 확정 후 조정할 수 있습니다**」라고 적혀 있어서,
2026-09-20 이은총 님 결정으로 **「상담 후 안내」**로 두었습니다.
원가가 확정되면 `SNS/매뉴얼0` 4장 가격표를 그대로 표로 넣으면 됩니다
(메인 세팅 200,000 · 단독 120,000 · 유튜브 110,000 · 네이버/구글 지도 각 100,000 ·
카카오맵/애플 각 50,000 · 티맵 30,000 · 샤오홍슈 400,000 · 계정 없으면 채널당 +30,000).

🔴 **부가세 표기를 「전부 포함」으로 통일했습니다** (2026-09-20 이은총 님 결정).
매뉴얼의 영상 가격은 **부가세 별도**였기 때문에, 화면에 올린 값은 제가 **×1.1 한 것**입니다.

```
           편집만    촬영+편집   기획+편집   기획+촬영+편집   (부가세 포함)
숏폼        27,500    82,500     115,500     170,500
홍보 스케치 165,000   363,000    385,000     583,000
홍보 인터뷰 220,000   440,000    440,000     660,000
정보전달    220,000   440,000    440,000     660,000
```

⚠️ **끝자리가 깔끔하지 않습니다.** 실제로 받으실 금액이 다르면 여기 숫자를 고쳐 주세요.
⚠️ **블로그 편당 6,000원은 부가세 표기를 안 붙였습니다** — 그 값이 포함인지 별도인지
확인된 적이 없고, `settings.blog.sale` 은 정산이 그대로 쓰는 값이라 함부로 바꾸면 안 됩니다.

### 견적서 = 계약서 겸용 (2026-09-20)

`/marketing/admin/` 과 `/hakwon/admin.html` **두 어드민 모두** 견적서 아래에
**작업 순서 · 포함되는 것 · 포함되지 않는 것 · 환불 규정 · 서명란**이 붙습니다.
의뢰 종류에 따라 매뉴얼 문구가 자동으로 채워지고, 고쳐 쓸 수 있습니다
(「기본 문구 다시 넣기」 단추 — 덮어쓰기 전에 확인을 묻습니다).

🔴 **비어 있는 칸은 인쇄되지 않습니다.** 조항이 하나도 없으면 서명란도 안 나옵니다.
   빈 제목만 찍히는 것을 막으려는 것입니다.

| 어드민 | 조항이 있는 것 | 비어 있는 것 (매뉴얼 없음) |
|---|---|---|
| 인투마케팅 | 영상 · SNS · 블로그 · 홈페이지 | **디자인** |
| 학원(ESC) | 자료제작(홈페이지) · 마케팅(블로그) | **인력지원 · 컨텐츠지원 · 자료제작 중 디자인** |

비어 있는 종류는 편집 화면에 빨간 글씨로 「매뉴얼이 없어 기본 문구가 없습니다 — 직접 적어 주세요」가 뜹니다.
조항 문구는 각 어드민 파일 안 `TERMS` 에 있습니다. 매뉴얼이 나오면 거기에 넣으면 됩니다.

### ✅ 블로그 판매가 10,000원 — 8번 창이 정산까지 반영 (2026-09-20)

8번 창에서 아래처럼 넣었고, 9번 창이 DB 로 확인했습니다.

```
판매가   일반 10,000 · 파트너 3,000
등급표   새내기 4,000 / 정규 4,500 / 숙련 5,000 / 우수 5,500 / 리더 6,000
검수     통과 1,000 + 확인 1,000
split    삭제됨 (원래 정산에서 안 쓰던 값)
```

· 일반 10,000 = 등급표 + 검수 2,000 + 공동체 운영비 1,000 + 센터
· 파트너 3,000 = 블로거 1,000(등급 없음) + 검수 500 + 공동체 0 + 센터 1,500
· 부가세는 센터 몫에서
· **과거 글은 안 바뀝니다** — 원고료는 배정할 때, 검수 수당은 검수할 때 글에 박히는 값이라
  기존 50편은 1,000/250/250 그대로. 판매가도 주문 만들 때 굳어 혼공기적은 그대로.
· `blog_pay_mult`(배수) 방식은 폐기되고 주문 갈래별 금액표로 바뀌었습니다.

🔴 **9번 창 쪽에 남아 있던 함정을 같이 고쳤습니다.**
두 어드민(`marketing/admin`, `hakwon/admin.html`)이 블로그 주문을 만들 때
**6,000원이 코드에 박혀** 있었습니다. 그대로 뒀으면 10,000원으로 올린 뒤에도
주문이 6,000원으로 만들어졌을 것입니다. 이제 `settings.blog.sale` 에서 읽습니다.

🔴 **못 읽으면 빈칸 + 빨간 경고**가 뜨게 했습니다. 기본값으로 숫자를 박아 두면
값이 또 바뀐 뒤 읽기가 실패했을 때 옛 금액으로 조용히 주문이 만들어집니다.
견적서 「기본 항목 넣기」도 같은 규칙입니다.

옛 기록 — 화면만 바꿨던 단계:

### 🔴 (지난 기록) 블로그 판매가 10,000원 — 화면만 바꿨고 정산은 그대로입니다

2026-09-20 이은총 님: 일반 **10,000원(부가세 포함)**, 파트너는 **3,000원 그대로**.
화면(인투마케팅 2곳·학원 3곳)은 바꿨습니다. **`settings.blog.sale` 은 6,000 그대로 두었습니다.**

이유 — `blog_pay_mult()` 가 **판매가(일반) ÷ 판매가(파트너)** 로 배수를 내고,
그 배수로 블로거 원고료가 정해집니다.

```
지금    6,000 ÷ 3,000 = 2배
올리면 10,000 ÷ 3,000 = 3.33배   ← 블로거 원고료가 조용히 1.67배가 됨
```

새로 정해진 분배(블로거 4,000 · 검수자 2,000 · 공동체 1,000 · 센터 3,000)도
지금 구조와 모양이 다릅니다.

| 새 분배 | 지금 실제 계산 |
|---|---|
| 블로거 4,000 | 등급별 단가(새내기 1,000 ~ 리더 3,000) × 배수 |
| 검수자 2,000 | 통과 250 + 확인 250 = **500** |
| 공동체 1,000 | `community_payouts` 에서 따로 |
| 센터 3,000 | 나머지 |

2026-09-20 조회 — 글 **50편 전부 payout_rate 1,000**, review_pay 250, verify_pay 250,
**blog_payouts 4건(이미 정산이 돌았음)**. 「언제 쓴 글부터 새 단가인가」를 정해야 합니다.
`settings.blog.split`(2·2·1·1)은 **정산 함수 어디에서도 쓰지 않습니다** — 옛 값입니다.

→ 블로그센터·정산은 8번 창 영역이라 **넘겼습니다.**

### 🔴 데모 주문 10건이 «진짜 DB» 에 들어 있습니다 (2026-09-20)

`/marketing/admin/` 화면이 비어 있어서 이은총 님 요청으로 넣었습니다.
**이름이 전부 `[데모]` 로 시작합니다.**

| 왼쪽 메뉴 | 건수 | 담당자 있음 | 입금됨 |
|---|---|---|---|
| 홈페이지 제작 · SNS 채널 제작 · 그래픽 디자인 · 영상 제작 · 블로그 · 리뷰 | 각 2 (합 10) | 각 1 | 각 1 |

🔴 **보고 숫자를 낼 때 반드시 빼세요.** 2026-09-12 에 데모가 섞여 숫자가 틀렸던 일이 있습니다.
`center='mkt'` 의뢰 10건은 **지금 전부 데모**이고, 진짜는 학원(`esc`) 2건뿐입니다.

한 줄로 전부 지웁니다.

```sql
delete from public.requests where academy_name like '[데모]%';
```

담당자로 이은총·전보배·박요엘 님이 들어가 있어 그분들 「내 작업」에도 보입니다.

### 아직 안 한 것

- **데모 주문 10건 지우기** (진짜 주문이 들어오기 전에)
- 블로그 정산 구조 반영 (8번 창)
- 디자인 · 인력지원 · 컨텐츠지원 매뉴얼 → 나오면 각 어드민 `TERMS` 에 넣기

---

## 2026-09-12 — 주소 구조를 새로 짜고, 어드민 셋을 만들었습니다

### 확정된 주소 구조

**① 고객이 보는 홈페이지 (= 플랫폼)**

| 주소 | 무엇 | 파일 |
|---|---|---|
| `intomarketing.co.kr/` | 인투마케팅 메인 | `index.html` |
| `intomarketing.co.kr/hakwon/` | 학원지원센터 | `hakwon/index.html` |
| `intomarketing.co.kr/church/` | 우리교회지원센터 | `church/` (148개) |

🔴 **서로 링크·버튼으로 연결하지 않습니다.** 손님에게는 남남인 홈페이지로 보여야 합니다.
그래서 학원 손님을 `intomarketing.co.kr/apply/` 로 보내지 않고 `hakwon/apply/` 를 따로 만들었습니다.

**② 신청서**

| 주소 | 넣는 값 | 파일 |
|---|---|---|
| `/apply/` | `center='mkt'`, `kind='홈페이지'` | `apply/index.html` |
| `/hakwon/apply/` | `center='esc'`, `kind=` 고른 값 | `hakwon/apply/index.html` |

**③ 어드민**

| 주소 | 무엇 | 파일 |
|---|---|---|
| `/admin/` | **모아 보기 전용. 고치는 기능 없음** | `admin/index.html` |
| `/marketing/admin/` | 메인 홈페이지에서 온 신청 | `marketing/admin/index.html` |
| `/hakwon/admin.html` | 학원 홈페이지에서 온 신청 | `hakwon/admin.html` |
| `/church/admin.html` | 교회 (계정·DB 따로) | `church/admin.html` |

`/marketing/` 에 고객 페이지는 없습니다. 어드민만 있습니다.

**④ `center.intomarketing.co.kr` = 블로그센터 전용.** 다른 센터 개념은 폐기했습니다.

---

### 🔴 다음 사람이 반드시 알아야 할 함정

**1. `center.intomarketing.co.kr` 루트를 절대 비우지 마세요.**
알림톡 템플릿에 주소가 박혀 있고, 카카오는 승인된 템플릿을 **한 글자도** 못 고치게 합니다.
바꾸면 19종 전부 재심사이고 하나당 3~5일입니다.

```
center.intomarketing.co.kr/            13곳   ← 블로그센터 (지금 그대로 두기로 함)
center.intomarketing.co.kr/status.html  5곳   ← 있음
center.intomarketing.co.kr/consent.html 1곳   ← 🔴 경로 확정(9/13). 화면은 아직 없음
center.intomarketing.co.kr/admin/       0곳   ← 알림톡에 없음
```

#### ✅ `consent.html` 경로 확정 (2026-09-13, 이은총 님)

```
https://center.intomarketing.co.kr/consent.html?k=#{동의키}
```

**만 14세 미만 블로거의 보호자에게 개인정보 수집 동의를 받는 화면**입니다.
알림톡 20번 `guardian_consent` 가 이 주소를 가리킵니다. 받는 사람은 블로거가 아니라 **보호자 휴대전화**.

🔴 **이 한 줄은 이제 바꾸면 안 됩니다.** 파일 이름·위치·`?k=` 이름 전부.
20번은 다른 19종과 순서가 반대라 — 기능이 없는 상태에서 문구를 먼저 정했으므로 —
**화면이 이 주소에 맞춰야** 합니다.

**만들 때 따를 본** — `status.html?k=` 와 같은 방식 (알림톡 문서가 권함)

| | `status.html` (있음) | `consent.html` (만들 것) |
|---|---|---|
| 열쇠 칸 | `blog_orders.share_key` | `bloggers.` 에 새 칸 필요 |
| 읽는 통로 | 공개 RPC `order_status` | 공개 RPC 새로 |

**DB — 2026-09-13 기준 있는 것 / 없는 것** (알림톡 문서 「함께 만들어야 하는 것」 대조)

| 칸 | |
|---|---|
| `bloggers.guardian_consent_at` | ✅ 있음 |
| `bloggers.guardian_note` | ✅ 있음 (문서엔 없던 칸) |
| `guardian_name` | ❌ 없음 |
| `guardian_phone` | ❌ 없음 — 발송 대상이라 필수 |
| `consent_method` | ❌ 없음 — 구글폼/센터 구분용 |
| 동의키 칸 | ❌ 없음 |

그 밖에 문서가 요구하는 것 — 가입 화면에서 만 14세 미만이면 보호자 이름·연락처 **필수** /
신청 접수 시 **자동 발송** / **동의 전까지 활동 시작 안 됨** / `#{공동체명}` 이 비면 `-`.

2026-09-13 기준 생년월일 있는 블로거 22명 중 **만 14세 미만 7명, 센터에 기록된 동의 0명.**
지금은 구글 폼(마감 9/21)으로 받고 있어, 폼으로 받은 동의를 `guardian_consent_at` 에 옮겨 적는 일도 남아 있습니다.

✅ **솔라피 답변 — 보호자(제3자)에게도 알림톡 발송 가능.** 이은총 님이 전에 문의해서 받은 답입니다
(2026-09-13 전해 들음). 날짜·원문은 문서에 기록이 없어 행정 창에 남겨 달라고 부탁했습니다.
알림톡 문서 20번의 「대행사에 먼저 물어볼 것」은 이미 해결된 항목입니다.

⚠️ **심사에 내기 전에 `consent.html` 화면부터 만들 것.** 심사 때 링크를 열어 볼 수 있는데
지금 그 주소는 404 입니다.

⚠️ `consent.html` 은 **블로그센터 저장소(`hakwon-blog`)** 에 들어갑니다. 이 저장소가 아닙니다.

**2. DB가 넷으로 갈라져 있습니다. 하나로 보면 안 됩니다.**

| 플랫폼 | Supabase |
|---|---|
| 인투마케팅 · 학원지원센터 | `qkvebwxewttqtcryfycy` |
| 우리교회지원센터 | `tbxosynzszcgtieonsui` |
| @IM (koreayjk.github.io/aim) | `lnrugwschlcesaaertgc` |

그래서 `/admin/` 모아 보기에 **교회 의뢰는 안 뜹니다.** 0건으로 보이지 않도록
숫자를 아예 만들지 않고 「교회 어드민 열기」만 뒀습니다. 0으로 쓰면 「의뢰가 없다」로 잘못 읽힙니다.

**3. 교회 사이트는 `main` 이 아니라 작업 가지에서 배포되고 있었습니다.**
`intoedu/wcsc` 의 GitHub Pages 원본 = `claude/church-support-platform-b296qn`.
`main` 을 옮겼으면 옛날 사이트를 옮길 뻔했습니다(index.html만 5.7KB 차이).
지금 `church/` 는 **살아있는 가지에서** 가져온 것이고 바이트까지 같음을 확인했습니다(45,165).

🔴 **wcsc 저장소는 아직 «작업 중»입니다. 보관 처리하면 안 됩니다.**

```
2026-09-12 07:27  관리자 사이드바 숫자를 '안 본 새 건'으로
2026-09-12 02:32  홈페이지 제작을 우리 신청서로 받습니다
2026-09-12 01:59  관리자 화면 — 지원 신청이 항목마다 따로 들어옵니다
2026-09-11 12:31  …
```

다른 창이 계속 교회 사이트를 만들고 있습니다. 지금 보관 처리하면 그쪽 밀어넣기가 막힙니다.

**그래서 지금은 교회 사이트가 두 곳에 있습니다.**

| | |
|---|---|
| `intoedu.github.io/wcsc/` | 개발이 일어나는 곳 (배포 가지 `claude/church-support-platform-b296qn`) |
| `intomarketing.co.kr/church/` | 2026-09-12 `74e328e` 를 떠온 사본 |

2026-09-12 기준 **둘의 내용은 같습니다** — 다른 줄 98개가 전부 미리보기 그림 주소이고
그 밖의 차이는 0 입니다. 다만 **wcsc 에 새 커밋이 생기면 이쪽이 바로 낡습니다.**

다시 맞추려면:

```sh
git clone -b claude/church-support-platform-b296qn https://github.com/intoedu/wcsc.git /tmp/wcsc
rsync -a --delete --exclude '.git' /tmp/wcsc/ <이 저장소>/church/
cd <이 저장소>/church
find . -type f \( -name '*.html' -o -name '*.js' \) -exec \
  sed -i '' 's|https://intoedu\.github\.io/wcsc/|https://intomarketing.co.kr/church/|g' {} +
```

### ✅ 정해졌습니다 (2026-09-12, 이은총 님) — 「wcsc 작업이 끝나면 보관」

그때까지는 **wcsc 가 개발처**, `intomarketing/church/` 는 배포 사본입니다.
wcsc 에서 일하는 창의 작업이 마무리되면 아래를 차례로 하세요.

1. **그 창에 끝났는지 확인.** 안 올린 작업이 남아 있으면 보관하는 순간 밀어넣기가 막힙니다.
2. **마지막으로 한 번 더 맞추기** (위 명령). 맞춘 뒤 반드시 확인할 것:
   ```sh
   # 주소 말고 다른 줄이 0 이어야 정상
   grep -rc 'intoedu.github.io/wcsc' church/ | grep -v ':0' | grep -v README
   ```
3. **커밋·푸시**하고 `intomarketing.co.kr/church/` 가 새 내용으로 뜨는지 눈으로 확인.
4. **그다음에** wcsc 보관 처리 — `gh api -X PATCH repos/intoedu/wcsc -F archived=true`
   (되돌릴 수 있습니다: `-F archived=false`)
5. wcsc README 맨 위에 「여기 아닙니다 → intomarketing.co.kr/church/」 한 줄.
   보관하면 못 고치니 **보관 전에** 넣으세요.

🔴 **순서를 지키세요.** 보관을 먼저 하면 5번을 못 하고, 맞추기 전에 보관하면
마지막 작업분이 `church/` 에 안 들어옵니다.

**4. 교회 폴더는 빌드 결과물입니다.**
`church/build.js` + `church/src/` 로 만들어집니다. 이 맥에는 `node` 가 없어 빌드를 못 돌렸습니다.
미리보기 그림 주소를 **결과물 24장과 원본 `church/src/data/site.js:16` 양쪽** 다 고쳐 뒀으니
다음에 누가 `node build.js` 를 돌려도 새 주소가 나옵니다.
`church/README.md` 에 옛 주소가 한 곳 남아 있는데 「~로 바꿨습니다」라는 지난 기록이라 그대로 뒀습니다.

**5. 미리보기 서버는 바탕화면을 못 읽습니다.**
macOS 가 막습니다(sandbox). 임시폴더로 **복사해서** 띄워야 합니다.
한글 폴더 이름을 파이썬 문자열로 쓰면 자모 분리(NFD) 때문에 또 못 찾습니다.

**6. 견적서 발행 주체가 둘입니다.**
`settings.quote.profiles[]` — `esc`(ESC 학원지원센터 / 인투유학), `mkt`(인투마케팅 / 306-24-53380).
학원 어드민은 **인투유학**, 인투마케팅 어드민은 **인투마케팅** 이름으로 자동 선택됩니다. 섞으면 안 됩니다.

**7. 각 어드민은 자기 `center` 만 읽어야 합니다.**
`hakwon/admin.html` 은 9/12 전까지 **모든 의뢰를 다 읽고** 있었습니다.
`area` 가 셋 중 하나인 것만 담아 우연히 안 샜을 뿐입니다. 지금은 `.eq('center','esc')` 로 거릅니다.

**8. 「기타」 묶음을 지우지 마세요.**
어느 묶음에도 안 들어가는 `kind` 가 오면 의뢰가 **화면에서 사라집니다.**

---

### DB — 오늘 더한 것 (전부 «더하기»만, 기존 칸·데이터 안 건드림)

```
20260910135005  requests_multi_center_and_production
                center · kind · method · stage · due_stages · amount · paid · paid_at · site_url
                + 공개 신청은 금액·입금·담당자·단계를 못 넣게 잠금
20260910135234  settings_centers_and_site_footer_price
20260910135312  site_public_rpc          공개 홈페이지가 가격·푸터를 읽는 통로
20260910140949  centers_left_menu
20260912132713  settings_platforms       플랫폼 목록 + 각 어드민 주소
```

`settings` 열쇠

| 열쇠 | 무엇 | 누가 고치나 |
|---|---|---|
| `platforms` | 플랫폼 목록 · 어드민 주소 · `data_here` | (아직 화면 없음 — DB에서) |
| `site.mkt` | 인투마케팅 푸터·가격 | `/marketing/admin/` → 홈페이지 정보·푸터 |
| `site.esc` | 학원 푸터·가격 (**사업자 두 줄**) | `/hakwon/admin.html` → 홈페이지 정보·푸터 |
| `quote` | 견적서 발행 주체 | 각 어드민 → 견적서 설정 |
| `blog` | 🔴 **블로그 판매가·분배. 정산이 이 값을 씁니다** | 블로그센터 |
| `centers` | 9/10 옛 구조. **안 쓰지만 지우지 않았습니다** | — |

🔴 **블로그 판매가는 `settings.blog.sale` 하나만 진짜입니다.** 어디에도 복사하지 마세요.
판매가를 바꾸면 관리자·검수자·블로거·공동체 분배도 다시 맞춰야 합니다.

---

### 오늘 바꾼 파일

```
M  index.html            인투마케팅 메인 (+812 −85)
M  hakwon/index.html     자료제작 → @IM 대신 우리 신청서로
M  hakwon/admin.html     center 거르기 · 자료제작 묶음 · 제작 진행 · #req= · 푸터 설정 (+198)
+  admin/                모아 보기 (고치는 기능 0)
+  apply/                인투마케팅 제작 신청서
+  marketing/admin/      인투마케팅 어드민 (학원 어드민을 본떠서 만듦)
+  hakwon/apply/         학원 자료제작 신청서
+  church/               우리교회지원센터 148개
```

`#req=<의뢰id>` 를 붙이면 그 의뢰가 바로 열립니다. `/admin/` 에서 줄을 누르면 이렇게 넘어갑니다.

---

### 못 한 것

- ~~`center.intomarketing.co.kr/admin/` 옛 파일~~ — **2026-09-12 지웠습니다.**
  한 번도 커밋·배포된 적이 없어서 실제 주소로는 존재한 적이 없습니다(지금도 404).
  블로그센터 루트와 `status.html` 은 그대로 200 입니다.
- **wcsc 보관 처리** — 「작업 끝나면 보관」으로 정해졌습니다(2026-09-12).
  아직 다른 창이 거기서 일하고 있어 보류 중입니다. **절차는 위 함정 3번에 적어 뒀습니다 —
  순서를 지키세요.**
- **`consent.html` 화면** — 경로는 **확정됐습니다**(2026-09-13). 화면·DB 칸 4개·공개 RPC 는 아직.
  블로그센터 저장소 일이라 어느 창이 맡을지 정해야 합니다 (위 함정 1번)
- **`settings.platforms` 를 화면에서 고치는 기능** — 지금은 DB에서 고쳐야 합니다
- **교회 DB 합치기** — 오늘은 안 하기로 했습니다. 하려면 `church/supabase.sql`(표 14개)을
  우리 프로젝트에 얹어야 하는데 `requests`·`settings` 이름이 겹칩니다. **먼저 상의할 것.**
